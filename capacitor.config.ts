import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.bongane.assignment',
  appName: 'Assignment',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
